# This workflow will build a golang project
# For more information see: https://docs.github.com/en/actions/automating-builds-and-tests/building-and-testing-go

name: Go

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:

  build:
    runs-on: golang:1.20
    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Install APT packages
      run: |
        sudo apt-get update
        sudo apt-get install -y libopus-dev=1.3.1-3 libopusfile-dev=0.12-4

    - name: Test
      run: go test -v ./...
